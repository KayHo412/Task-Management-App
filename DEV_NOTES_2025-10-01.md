# Developer Notes – Week of 2025-10-01

## Key Fixes/Changes
- Updated GitHub Actions workflow to set backend Docker build context to `./backend`
- Backend Dockerfile now uses:
  - `COPY package.json package-lock.json ./`
  - `COPY . .`
- `.env.example` should be present in `backend/` for Docker build
- All team members should pull latest `main` and resolve merge conflicts (especially in Dockerfile)

## Why it Matters for Deploy
- Docker builds will fail if context or file locations are wrong
- Deployment scripts should use the same context and Dockerfile logic as CI

## Helpful Links
- [PR #20: Change context for build docker for backend](https://github.com/5G00DM04-3007/course-project-hopeitworks/pull/20)
- [Current main_pipeline.yml](./.github/workflows/main_pipeline.yml)

## Deployment Checklist
- [ ] Backend Docker build context is `./backend`
- [ ] `.env.example` in place before build
- [ ] Dockerfile uses correct copy commands
- [ ] All developers have pulled and resolved conflicts
- [ ] Run a test build locally before production deploy

---

